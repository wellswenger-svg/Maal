# Rejected eval batches (do not promote to gold)

Phase0 v1 (`d0.86`), v2 (crop + region_lock), and related GPU batches failed visual gates:
face/hair drift, nipple poke, cloth junk, or no real bust volume vs refs.

Interim next step: Kontext LoRA swap — see `../PATH_B_THEN_A.md`.
Older audit notes remain in `AUDIT.md`.
