# Maal

Private ComfyUI orchestration for image-to-image and image-to-video.

Action presets and extra weight maps are configured at runtime (gitignored `private/` or the API after PIN unlock). They are not stored in this repository.

PIN auth is required to use the app.
